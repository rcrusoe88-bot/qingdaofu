# 清道夫发布记录

## v1.1.1 — 安全、回执与发布基线修复

发布日期：2026-09-22。

### 发行基线

- 基于 v1.1.0 的实际发行内容与后续 main 修整，保留全部 **82 条规则**及其 11 条新增规则定义。
- 6 条旧规则仅禁用、不删除：AWS、Azure、Kubernetes、Steam、Office、Zoom。启用 75 条清理规则与 1 条只读规则。
- 上一轮基于 71 条规则的本地验证包不是发行产物，不应替换 v1.1.0。

### 修复

- 阻止候选路径及祖先目录中的重解析点；永久删除改为固定父目录并通过文件句柄校验和删除。
- 清理前建立回执，逐文件写入持久化计划/结果；中断记录可恢复，未知结果明确标记。
- 停止与关闭窗口改为先请求停止并保存记录，不再直接杀掉正在清理的后台任务。
- 新回执使用无 BOM UTF-8，兼容读取旧 BOM 回执；错误后恢复 GUI 状态。
- 后台原子占位，避免同一窗口重复启动；分块传输大结果并处理截断/错误。
- 禁用旧分析器删除入口；冲突规则不再通过放宽保护解决。
- 修正测试结果判断、实际 GUI 的 CI 覆盖、版本号以及正式打包/发布入口。
- README 与使用说明不再建议无条件绕过安全软件。

### 验证与产物

- Windows PowerShell 隔离回归、Go 后端/旧工具测试、go vet、前端状态测试及 Wails 构建。
- 实际 ZIP 验证：82 条规则 ID、11 条新增规则定义、版本、完整文件清单与 SHA-256。
- 下载：`qingdaofu-1.1.1-x64.zip` 与 `SHA256SUMS.txt`。源提交与包内哈希见 `BUILD-MANIFEST.json`。
- 只在隔离人工文件上执行删除测试；不宣称已验证真实用户缓存、全部第三方应用或所有回收站交互。Go race 未运行。

### 已知限制

回收站使用 Windows 路径接口，不能宣称完整抵御恶意同用户进程竞态；系统强制结束可能留下“未知”结果。未签名程序的报警需核实，哈希不是安全认证。

## v1.1.0 — 新增 11 条规则

2026-09-22 发布。规则从 71 增至 82，新增 uv、Claude 桌面版/虚拟机镜像、WebView2、ima.copilot、WPS、火绒应用商店、浏览器 Crashpad、Cherry Studio、Antigravity 及更新器残留安装包规则。

## v1.0.1

2026-09-21 发布。修复多规则勾选时的 PowerShell 参数传递问题。

## v1.0.0

2026-09-21 首次公开发布。

旧 Mole 发布指南归档于 `docs/legacy-mole-release-guide.md`，不作为清道夫发布流程使用。
